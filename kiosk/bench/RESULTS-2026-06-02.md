# Kerchunk Kiosk — Hardware Bench Results (2026-06-02)

Run on the Raspberry Pi 4 (this repo's host), RTL-SDR RTL2838UHIDIR (R820T tuner),
one HDMI display connected on card 1 (`vc4hdmi1`). Functional bench — the real
backend + real dongle run locally (not via systemd/the `kerchunk` user).

## Results

| Step | Result | Notes |
|------|--------|-------|
| B1 — Dongle detected | **PASS** | `rtl_test -t` found RTL2838UHIDIR + R820T, no `usb_claim_interface` error. |
| B2 — PCM-energy detection | **PASS** | Real PCM through `rmsAmplitude`: NOAA 162.550 MHz (active) ≈ **2926 RMS (p50)**, max ~4639; quiet 144.000 MHz (noise floor) ≈ **150** (max spike ~1436). Clean separation — the energy-based detection model works on real signal. Standalone `rtl_fm` streamed 78 KB in 3 s as expected (12 kHz × 16-bit). |
| B3 — Dashboard on boot | **NOT RUN** | Requires systemd install + physical screen observation. Hand-off step. |
| B4 — HDMI audio + Now Playing | **BLOCKED** | See findings 2 & 3 below — could not get a clean full-app run (device kept dropping; no usable audio sink headless). |
| B5 — Live volume/mute | **BLOCKED (hardware)** | HDMI cards expose no `amixer` control (finding 2). |
| B6 — Dongle loss + recovery | **PARTIAL** | Recovery via replug worked (device re-enumerated cleanly, tuner found). The loss itself was triggered involuntarily by repeated open/reset cycles (finding 1). |
| B7 — Reboot persistence | **NOT RUN** | Requires reboot + screen observation. Hand-off step. |

## Findings (and the fixes applied on this branch)

### 1. This RTL2838 drops off the USB bus after repeated open/reset cycles
Cold, it enumerates and streams fine. After several rapid `rtl_fm` start/stop/reset
cycles (as a bench loop produces) it disappears from `lsusb` entirely and needs a
physical replug; a software unbind/bind does **not** re-enumerate it. This is a
known trait of cheap RTL dongles, not a software bug — but it makes an automated
full-bench loop unreliable. *Mitigation in code:* the engine now surfaces the
device-gone state as a `NO_DEVICE` error event (finding 3) instead of silently
spinning.

### 2. HDMI audio exposes no `amixer` control; headless `aplay` to default fails
Neither HDMI card (0 `vc4hdmi0`, 1 `vc4hdmi1`) has any `amixer` simple control —
only the headphone jack (card 2) has a `PCM` control. The engine called
`amixer -c 0 sset Master …`, which can't succeed on HDMI here. Separately,
`aplay` to the default device failed headless with `audio open error: Unknown
error 524` (no audio session). *Fix on this branch:* `aplay` is now pointed at
the configured `audio.sink` via `-D`, and `amixer` volume/mute degrade to a safe
no-op when no mixer control exists (instead of erroring / risking the boot chain).
Name→card-index mapping and control discovery remain a documented limitation.

### 3. Squelch config default sat on the noise floor
Default `squelchLevel: 150` ≈ the measured noise floor → constant false
squelch-opens. *Fix on this branch:* default raised to **1800** (clears noise
~150–1436, well below signal ~2900). Operators can still set any value.

### 4. `rtl_fm` stderr was discarded
The engine spawned `rtl_fm` with stderr ignored, so "No supported devices found"
(the exact symptom of finding 1) produced no useful signal. *Fix on this branch:*
the engine captures rtl_fm stderr and, on unexpected exit, emits a `NO_DEVICE`
error (dongle missing/wedged) vs a generic `RTL_EXITED`, including the captured
reason in the message.

## Still to verify on real hardware (after these fixes, on a fresh device)
- B4 end-to-end: confirm the dashboard shows ACTIVE with the correct freq/tag on a
  live signal, and PCM reaches a real sink (use the headphone jack — which has a
  working mixer — or a confirmed-working HDMI audio path).
- B3 / B7: install the systemd units and verify dashboard-on-boot + reboot
  persistence with a screen attached.
- Re-confirm the engine's stdout→detector path on a non-wedged device (the
  in-session "no events" observations coincided with a device that had already
  dropped off the bus, so they are inconclusive).
